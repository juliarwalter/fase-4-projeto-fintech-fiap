const botaoMenu = document.querySelector(".menu")
const menu = document.querySelector("nav.menu_lateral")

botaoMenu,addEventListener(click, () =>{
    menu.classList.toggle("nav.menu_lateral--active")
})
